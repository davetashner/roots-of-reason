# Example scenario demonstrating assertion commands.
# Run with: ror scenario --file tests/scenarios/example_assertions.scenario
# Requires a running game with --debug-server.

# Set game speed for faster execution
speed 3

# Verify the game is running
assert time>=0

# Check that we have at least one player unit
assert-count --owner 0 >= 1

# Take a screenshot to verify initial state
screenshot example_start --annotate
